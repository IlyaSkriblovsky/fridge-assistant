# Контракт с прибором

Контракт `/audio` реализован сервером и прошивкой; его описание со стороны
прибора — в [project-vision.md](../../firmware/docs/project-vision.md).
Эндпоинт `GET /sticky/dashboard` реализован в обеих частях; проверка на приборе
ещё предстоит.
Если контракт меняется, он меняется в обеих частях проекта сразу.

Здесь только сам контракт. То, что прошивка пока не умеет и сколько она ждёт
ответа, — это её недоработки и настройки, а не ограничения сервера. Прошивку
подгоняют под сервер, а не наоборот. Исключений сейчас нет: кириллицу прибор
рисует, длинный ответ переносит по словам.

## Запрос

```
POST /audio
Authorization: Bearer <токен>
Content-Type: audio/wav
Transfer-Encoding: chunked

<WAV: PCM, 16 kHz, mono, signed 16-bit little-endian>
```

- **Тело идёт потоком, пока кнопка зажата**, подробнее в следующем разделе.
- **Оба поля длины в WAV-заголовке равны `0xFFFFFFFF`**, потому что заголовок
  уходит раньше, чем запись получает длину. Настоящую длину сервер берёт из
  конца chunked-тела. `0xFFFFFFFF`, а не ноль, потому что Python `wave` читает
  такой файл как файл неизвестной длины, а ноль отвергает как `not a WAVE file`.
- **Поток может оборваться посередине.** Тогда до сервера доходит обрезанное
  тело, и прибор уже показывает ошибку.
- **Токен статичный и общий у прибора и сервера**: `kDeviceToken` в
  `src/secrets.h` прошивки и `DEVICE_TOKEN` у сервера, см.
  [vision.md](vision.md#один-пользователь-один-прибор-один-токен). Без него
  сервер дочитывает тело и отвечает `401`, прибор показывает
  `SERVER ERROR 401`.

## Звук приходит потоком

Запрос открывается, как только прибор подключился к WiFi и нажатие длится
достаточно долго, чтобы считаться вопросом. Дальше звук уходит на сервер по
мере записи, пока пользователь ещё говорит. Последний (нулевой) chunk означает,
что вопрос закончен. Отдельного сигнала «я всё» нет.

Поэтому сервер может не ждать конца вопроса: входящий поток можно сразу
передавать во внешний сервис, например в потоковое распознавание речи. Тогда к
моменту, когда пользователь отпустил кнопку, большая часть работы уже сделана,
и после этого остаётся дослать последние куски и дождаться результата. Конец
входящего потока становится сигналом «звук закончился» для внешнего сервиса.

Параметры звука известны с первых байт: они в WAV-заголовке, который приходит
первым. Если внешнему сервису нужен сырой PCM, заголовок можно отрезать.

Если перед сервером когда-нибудь встанет прокси, у него должна быть выключена
буферизация тела запроса (в nginx `proxy_request_buffering off`). Иначе поток
незаметно превратится в обычный POST, и всё это преимущество пропадёт.

## Ответ

```json
{ "response": "текст для экрана" }
```

Прибор читает только поле `response`, остальные поля игнорирует.

Текст он переносит по словам сам, так что перевод строки в ответе — это
намеренный разрыв абзаца, а не то, чем разбивают длинную фразу. На экран
помещается семь строк 24 pt, примерно 375 байт русского текста; что не
влезло, обрывается многоточием, и сервер об этом никак не узнаёт. Длину
ответа держит системный промпт, а не контракт.

Любой ответ 200 со строковым `response` прибор считает успехом и просто
показывает текст. Если серверу есть что сказать об ошибке (не понял вопрос,
внешний сервис недоступен), он говорит это текстом в `response`. Ответить
именно ошибкой, но с текстом, сервер пока не может, это отложено
([deferred.md](deferred.md#ошибки-как-обычный-ответ)).

## Что видит пользователь при сбое

| Что случилось | Экран |
| --- | --- |
| Сервер недоступен или соединение умерло посреди запроса | `NO SERVER` |
| Ответ не 200 | `SERVER ERROR` и код |
| Ответ не JSON или в нём нет строкового `response` | `BAD RESPONSE` |
| Прибор не дождался ответа | `TIMED OUT` |

Сколько прибор ждёт ответа, задаёт `kResponseTimeoutMs` в `src/config.h`
прошивки. Если серверу понадобится больше, это значение увеличивают.

Чтобы вызвать эти случаи нарочно, у сервера есть отдельные эндпоинты:
[server.md](server.md#намеренные-сбои).

## Дашборд

Реализован 2026-09-22. Отдельный запрос за картинкой; контракт `/audio`
сохраняется. Сценарий, обработка сбоев и границы задачи описаны в
[idle-screen.md](use-cases/idle-screen.md).

### Запрос картинки

```http
GET /sticky/dashboard?battery_pct=76&temperature_c=23.4&humidity_pct=48.2
Authorization: Bearer <токен>
Accept-Encoding: identity
```

Параметры — свежие показания прибора: заряд в процентах (целое 0–100),
температура в °C и относительная влажность в процентах (0–100).
Дробные значения передаются с десятичной точкой; отрицательная температура
допустима. Недоступный датчик — пропущенный параметр, а не ноль или `NaN`.
Некорректные значения сервер отклоняет. Токен тот же, что для `/audio`,
через общую зависимость авторизации.

Эндпоинт всегда возвращает однобитный PNG. Параметра `format` нет.
Ответ имеет `Cache-Control: no-store`.

PNG можно открыть прямо в браузере: на запрос без авторизации сервер отвечает
Basic challenge; логин `sticky`, пароль — тот же `DEVICE_TOKEN`. Bearer также
поддерживается. Basic действует только для `/sticky/dashboard`;
URL никогда не содержит токен.

### Ответ картинки

```http
HTTP/1.1 200 OK
Content-Type: image/png
Dashboard-Format: png
Content-Length: <размер PNG в байтах>
Next-Update-After: 3600

<PNG-файл>
```

Единственный формат — PNG, который Pillow создаёт из изображения в режиме `1`:

- размер 800×480, grayscale (color type 0), bit depth 1;
- без interlace, палитры и прозрачности; чанки IHDR, IDAT и IEND;
- пиксель PNG `0` — чёрный, `1` — белый;
- координаты относятся к видимому экрану в текущей ориентации: кнопки сверху;
- прошивка распаковывает PNG в буфер 48 000 байт с полярностью для отрисовки.

Сервер не применяет дополнительное сжатие через `Content-Encoding`.
Требуется точный `Content-Length`, chunked-ответ не поддерживается.
Прошивка ограничивает PNG 65 536 байтами, проверяет статус, заголовки,
полное получение тела, CRC чанков, zlib checksum, размеры и формат картинки.
Только полностью проверенное изображение передаётся дисплею. Ошибочный,
избыточный или оборванный ответ не меняет основное содержимое экрана.

### Next-Update-After

Наш заголовок ответа, целое положительное число **секунд** до следующего
запроса. Точка отсчёта — момент полного получения тела корректного ответа,
зафиксированный до отрисовки. Время вывода картинки и остальной работы уже
входит в интервал: перед deep sleep прошивка ставит таймер на оставшуюся
часть, а не на исходное значение. Изображение сначала должно дорисоваться.

Без заголовка или с некорректным значением используется час (3600 секунд).
Прошивка должна проверять переполнение и ограничивать интервалы допустимыми
пределами из `config.h`, чтобы ответ не мог создать непрерывные пробуждения
или отключить обновления на неопределённый срок. Пределы — от 60 до 86 400 секунд; корректные числа вне диапазона
ограничиваются этими границами, ноль, нечисловой ввод и переполнение дают час.
Бюджет Wi-Fi — 15 секунд; HTTP — 15 секунд с таймаутом соединения 5 секунд.
При отмене до открытия сокета задача завершает текущую попытку соединения
в пределах сетевого таймаута; голосовая запись её не ждёт.
Это настройки, а не замеры аппаратуры.
Просроченный срок требует ближайшей допустимой попытки без быстрого цикла
повторов. После сбоя загрузки следующая попытка — через час.

Срок переживает глубокий сон и промежуточное переключение звука; новый
корректный ответ заменяет его. Это расписание опроса, а не срок актуальности
данных и не команда автономного напоминания. Длительность показа голосового
ответа пока задаётся только константой 10 секунд в прошивке.

### Области локальных индикаторов

Макет оставляет белыми прямоугольники `(x=176, y=8, w=48, h=32)` для звука
и `(x=240, y=8, w=32, h=32)` для неудачного обновления. Прибор накладывает
значки сам. Значок сбоя показывается только поверх известного дашборда;
после успешного получения кадра исчезает. Неизвестные старые пиксели не
перерисовываются частично. Остальные пиксели принадлежат серверу.
