# Assistant Server

Бэкенд персонального голосового ассистента. Прибор Seeed reTerminal Sticky
(E1005) на холодильнике присылает голосовой запрос на `POST /audio`, сервер его
выполняет, в том числе во внешних сервисах, и отвечает текстом, который прибор
показывает на ePaper. Один пользователь, один прибор, без авторизации: так
задумано.

Сейчас это прототип: принимает WAV, сохраняет на диск, отвечает фиксированной
фразой. AI выбран — Gemini API (`generateContent`, модель
`gemini-3.5-flash-lite`, см. `docs/vision.md`), но в сервер ещё не подключён.
Интеграций пока нет.

## Где что

| Путь | Что |
| --- | --- |
| `main.py` | Весь сервер: FastAPI, `POST /audio`, намеренные сбои |
| `pyproject.toml`, `uv.lock` | Зависимости, ставятся через `uv` |
| `recordings/` | Записи с прибора. В `.gitignore` |
| `experiments/` | Пробные скрипты, не часть сервера. Зависимости объявлены в самом скрипте (PEP 723), запуск: `uv run experiments/<script>.py` |
| `docs/vision.md` | Замысел, прибор, принятые решения, список сценариев |
| `docs/device-contract.md` | Контракт с прошивкой: запрос, потоковое тело, ответ, ошибки |
| `docs/server.md` | Как устроен сервер сейчас: запуск, переменные окружения, эндпоинты, лог |
| `docs/use-cases/` | По файлу на сценарий. Пока только `shopping-list.md` |
| `docs/open-questions.md` | Что не решено: уровень Gemini API, сервис для списка, хостинг, хранение записей |

Прошивка прибора — отдельный репозиторий, локально лежит в `../sticky-test-2`.
Первоисточник контракта — её `docs/project-vision.md` и `src/config.h`.

## Команды

```sh
uv sync                    # зависимости
uv run python main.py      # сервер на 0.0.0.0:8000, печатает URL для прибора
curl -X POST --data-binary @recordings/<file>.wav -H "Content-Type: audio/wav" \
  http://localhost:8000/audio
```

## Что важно не сломать

- **Контракт `/audio` общий с прошивкой.** Если он меняется, меняются оба
  проекта. Прибор читает из ответа только строковое поле `response`, и
  отсутствие этого поля он показывает как ошибку.
- **Недоработки прошивки — не ограничения сервера.** Кириллица, длина текста
  на экране, таймаут ожидания ответа: прошивку подгоняют под сервер, а не
  наоборот. Не транслитерируй и не обрезай ответы ради текущей прошивки.
- **Тело приходит потоком, пока кнопка зажата, и длины в WAV-заголовке равны
  `0xFFFFFFFF`.** Не буферизуй тело целиком и не доверяй длинам из заголовка.
  Поток можно передавать во внешний сервис, не дожидаясь его конца, но пока
  этого не делаем: `generateContent` получает запись целиком, когда тело
  закончилось. Потоковая передача понадобится, если перейдём на Live API.
- **Эндпоинты `/audio/fault/*` дочитывают тело до конца, прежде чем ответить.**
  Иначе прибор покажет `NO SERVER` вместо проверяемого сбоя.
- **Авторизации и нескольких пользователей нет намеренно.** Не добавляй их без
  запроса.
- **Открытые вопросы не решаются молча.** Сервис для списка покупок, хостинг,
  уровень Gemini API, а также смена AI, модели или перехода на Live API — это
  решения пользователя. Предлагай варианты, а принятое решение
  записывай в `docs/vision.md` или в документ сценария и удаляй вопрос из
  `docs/open-questions.md`.

## Документация

Документы в `docs/` написаны на русском, код и комментарии в нём — на
английском. Когда меняется поведение сервера, обнови `docs/server.md`. Новый
сценарий получает свой файл в `docs/use-cases/` и строку в таблице в
`docs/vision.md`.
