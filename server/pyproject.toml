[project]
name = "assistant-server"
version = "0.1.0"
description = "Server side for the Seeed Sticky (reTerminal E1005) AI assistant"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.34",
    "google-genai>=2.24.0",
    "gkeepapi>=0.17.1",
    "httpx>=0.28.1",
]
